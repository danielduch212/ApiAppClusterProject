name: Deploy Api Docker Image to ECR

on:
  workflow_dispatch:

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:
    - name: Ckeckout repository
      uses: actions/checkout@v4

    - name: Log to amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1
    
    - name: Build the Docker image
      run: |
        docker build . \
          --file Dockerfile \
          --tag ${{ steps.login-ecr.outputs.registry }}/my-api-repo:latest
          
    - name: Push to amazon ECR
      run: docker push ${{ steps.login-ecr.outputs.registry }}/my-api-repo:latest

  
